BOT_TOKEN=123456789:your-bot-token-from-botfather
PORT=5000
# Only ever set this to true on your own machine while building the UI
# without a real Telegram session. Never deploy with this on.
SKIP_AUTH_CHECK=false
